= provide :page_title, t('shared.menu.books')
= currently_at('Books')

h1 = t 'shared.menu.books'

= link_to t('.add_book'), new_book_path, class: 'btn btn-primary btn-lg mb-3'

= pagination @pagy

table class=%i[table table-striped table-hover]
  thead
    tr
      th class=[:col] Название
      th class=[:col] Автор
      th class=[:col] Описание
  tbody
    = render @books

= pagination @pagy
